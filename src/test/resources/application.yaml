spring:
  datasource:
    url: jdbc:tc:postgresql:15://localhost:5555/accounts-db
account-management:
  validation:
    sources: mail=firstName,email;mobile=phoneNumber;bank=bankId,lastName,firstName,middleName,birthDate,passportNumber